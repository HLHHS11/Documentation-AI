## `PythonSymbolInfo.__init__`のソース定義

```python
def __init__(self, namespace: str, symbol_name: str):
    self.namespace: str = namespace
    self.symbol_name: str = symbol_name
```

## `PythonSymbolInfo.__init__`の依存関係

依存する外部シンボルのドキュメントは以下の通りです。

- 依存する外部シンボルはありません。